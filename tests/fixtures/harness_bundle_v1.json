{
  "schema": "planledger.plan_bundle.v1",
  "request": {
    "title": "Add context export",
    "body": "I want planledger to export context for an LLM harness.",
    "planning_mode": "full"
  },
  "goal": {"title": "Make planledger harness-ready", "reuse": "active-or-create"},
  "initiative": {"title": "Machine-first context export", "reuse": "active-or-create"},
  "plan": {
    "title": "Context export MVP",
    "context": ["Harness needs stable JSON state."],
    "objectives": ["Export active records and next action."],
    "non_goals": ["No live LLM calls."],
    "validation_strategy": ["Unit tests and CLI smoke test."],
    "rollback_strategy": ["Remove created records from run if needed."]
  },
  "milestones": [
    {
      "title": "Context API",
      "slices": [
        {
          "key": "context-export",
          "title": "Add context export command",
          "objective": "Provide stable JSON context for harnesses.",
          "target_files": ["planledger/context.py", "planledger/cli.py"],
          "implementation_steps": ["Implement export_context", "Wire CLI command"],
          "acceptance_criteria": ["Command returns schema field", "Includes active initiative"],
          "validation_commands": ["python -m pytest tests/test_context_export.py -q"],
          "ready_for_taskledger": true
        }
      ]
    }
  ],
  "decisions": [
    {
      "title": "Keep LLM calls outside planledger",
      "decision_type": "architecture",
      "status": "accepted",
      "rationale": "Deterministic tests and provider-neutral design.",
      "options": [
        {"title": "Embed LLM provider", "status": "rejected"},
        {"title": "Harness owns LLM", "status": "accepted"}
      ]
    }
  ],
  "risks": [
    {
      "title": "Context export may become too large",
      "impact": "medium",
      "likelihood": "medium",
      "mitigation": "Default to summaries, add --include-bodies when needed."
    }
  ]
}
